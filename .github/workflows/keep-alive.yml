name: Keep Render backend awake
on: {schedule: [{cron: '*/5 * * * *'}], workflow_dispatch: {}}
jobs: {ping: {runs-on: ubuntu-latest, steps: [{name: 'Ping health endpoint', run: 'curl -fsS --max-time 20 https://e-commerce-springboot-umj0.onrender.com/actuator/health || true'}]}}
